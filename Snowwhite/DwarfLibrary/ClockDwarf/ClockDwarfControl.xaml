<UserControl
    x:Class="DwarfLibrary.ClockDwarf.ClockDwarfControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <RelativePanel>
        <TextBlock x:Name="time" Text="{x:Bind Time, Mode=OneWay}" RelativePanel.AlignTopWithPanel="True" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="24"/>
        <TextBlock x:Name="date" Text="{x:Bind Date, Mode=OneWay}" RelativePanel.RightOf="time" HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="24"/>
    </RelativePanel>
    
</UserControl>
