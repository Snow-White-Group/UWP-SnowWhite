<UserControl
    x:Class="DwarfLibrary.NewsDwarf.NewsDwarfControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DwarfLibrary.NewsDwarf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ListView x:Name="NewsList"
              ItemsSource="{x:Bind News}">
        <ListView.ItemTemplate>
            <DataTemplate
                x:DataType="local:NewsModel">
                <StackPanel Orientation="Horizontal" Padding="16">
                    <Ellipse Width="100" Height="100">
                        <Ellipse.Fill>
                            <ImageBrush ImageSource="{x:Bind ImageUrl}"/>
                        </Ellipse.Fill>
                    </Ellipse>
                    <StackPanel Orientation="Vertical" Width="250" Height="100" Margin="0,0,8,0">
                        <TextBlock Text="{x:Bind Headline}" FontSize="12px"/>
                        <TextBlock Text="{x:Bind ShortLine}" FontSize="10px" MaxLines="3"/>
                        <TextBlock Text="{x:Bind Source}" FontSize="9px" TextAlignment="Right"/>
                    </StackPanel>
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
        
    </ListView>
</UserControl>
